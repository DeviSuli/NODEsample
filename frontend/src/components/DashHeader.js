import { Link } from "react-router-dom";

const DashHeader = () => {
  const content = (
    <header className="dash-header">
      <div className="dash-header__constainer"></div>
    </header>
  );

  return <div>DashHeader</div>;
};

export default DashHeader;
